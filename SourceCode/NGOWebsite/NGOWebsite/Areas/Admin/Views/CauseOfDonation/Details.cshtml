@{
    ViewBag.Title = "Details";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

@model Models.CauseOfDonation

<div class="row" style="margin-top:30px">
    <div class="col-lg-12">
        <div class="panel panel-primary">
            <div class="panel-heading" style="font-weight:bold">
                <i class="fa fa-th  fa-fw"></i> Cause Details
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-8">
                        <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                            <tr>
                                <th>
                                    @Html.LabelFor(model => model.Id)
                                </th>
                                <td>
                                    @Html.DisplayTextFor(model => model.Id)
                                </td>
                            </tr>
                            <tr>
                                <th>
                                    @Html.LabelFor(model => model.Description)
                                </th>
                                <td>
                                    @Html.DisplayTextFor(model => model.Description)
                                </td>
                            </tr>

                            <tr>
                                <th>
                                    @Html.LabelFor(model => model.IsFieldOfPrograms)
                                </th>
                                <td>
                                    @if (Model.IsFieldOfPrograms == 1)
                                    {
                                        @Html.DisplayName("Yes")
                                    }
                                    else
                                    {
                                        @Html.DisplayName("No")
                                    }
                                </td>
                            </tr>

                        </table>

                    </div>
                    <div class="col-lg-4">
                        <a class="btn btn-primary" style="width:120px" href="@Url.Action("Edit", "CauseOfDonation", new { id = Model.Id })"><i class="fa fa-edit  fa-fw"></i> Update </a><br />
                        <a class="btn btn-danger" style="margin-top:15px;width:120px" href="@Url.Action("Delete", "CauseOfDonation", new { @id = Model.Id })" onclick="return confirm('Are you sure?');"><i class="fa fa-times-circle  fa-fw"></i> Delete </a><br />
                        <a class="btn btn-success" style="margin-top:15px;width:120px" href="@Url.Action("ListCause", "CauseOfDonation")"><i class="fa fa-mail-reply  fa-fw"></i> Back to list </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
