@model IEnumerable<Models.Partners>

@{
    ViewBag.Title = "NGO Website";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}
<div class="mask">
    <div id="intro-loader">
    </div>
</div>

<div id="slides">
    <div class="slides-container">
        <img src="~/Content/View_components/images/slider/slider1.jpg" alt="slider1">
        <img src="~/Content/View_components/images/slider/slider2.jpg" alt="slider2">
        <img src="~/Content/View_components/images/slider/slider3.jpg" alt="slider3">
    </div>
</div>
<div id="top" class="top">
    <div class="intro">
        <div class="container">
            <div class="row">
                <div class="col-md-3 text-center animate_left">
                    <div class="logo">
                        <img src="~/Content/View_components/images/logo.png" alt="logo">
                    </div>
                    <span class="subtitle">Education for all.</span>
                </div>
                <div class="col-md-9 slider_title">
                    <h2 class="animate_top">Supporting <span id="pageLoad">0</span> child education, and counting...</h2>
                    <a class="top_btn" href="@Url.Action("Donation", "User")">Donate Now</a>

                </div>
            </div>
        </div>
    </div>
</div>
@* ============================================== *@
<div id="middle" class="middle">

    <div class="container">
        <div class="row">
            <div class="col-md-8 text-center heading animate_top">
                <span>PROGRAMS</span>

                @{IEnumerable<Models.Programs> prolist;
                  prolist = ViewData["programs"] as IEnumerable<Models.Programs>;
                }
                @{var flag = 0;}
                @if (prolist != null)
                {
                    foreach (var item in prolist)
                    {
                        flag++;
                        if (flag % 2 != 0)
                        {
                    
                    <img src="~/Content/View_components/images/activity/act4.jpg" alt="img" class="animate_top">

                @*<img src="@item.Logo" alt="img">*@
                       
                    <h1>@item.Name</h1>

                            string[] arrListStr = item.Contents.Split(new char[] { '.' });
                            if (arrListStr.Count() > 2)
                            {
                    <h3>@String.Concat(arrListStr[0], arrListStr[1])</h3>
                            }
                            else
                            {
                    <h3>@item.Contents</h3>
                            }


                        }
                        else
                        {
                    <div class="row t1">
                        <div class="col-md-9 animate_right">
                            <h1>@item.Name</h1>

                            @{ string[] arrListStr = item.Contents.Split(new char[] { '.' });
                               if (arrListStr.Count() > 2)
                               {
                                <h3>@String.Concat(arrListStr[0], arrListStr[1])</h3>
                               }
                               else
                               {
                                <h3>@item.Contents</h3>
                               }
                            }

                        </div>


                        <div class="col-md-3 animate_left">
                            @*<img src="~/Content/View_components/images/organizers/org1.jpg" alt="img">*@
                        </div>
                    </div>
                        }
                    }
                }
            </div>


            <div class="col-md-4 slider_title">
                <h1>NEWS</h1>
                @{IEnumerable<Models.Informations> news;
                  news = ViewData["infor"] as IEnumerable<Models.Informations>;
                }
                @if (news != null)
                {
                    foreach (var i in news)
                    {
                    <h1>@i.Subject</h1>
                    <h3>Content</h3>
                    <p>@i.Contents</p>
                                       
                    }
                }

            </div>
        </div>
    </div>
</div>


@*<div class="container">
        <div class="row">
            <div class="col-md-1"></div>
            <div class="col-md-10 welcome_txt text-center animate_bottom">
                <p>
                    Lorem Ipsum has been the industry's standard dummy text ever since the <span>1500s</span>, when an unknown <span>printer</span> took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining unchanged. It was <span>popularised</span> in the 1960s.
                </p>
            </div>
            <div class="col-md-1"></div>
        </div>
    </div>*@
<div class="stories" id="stories">
    <div class="container text-center">
        <div class="row">
            <div class="col-md-12 heading dark animate_top">Our <span>Partners</span></div>
        </div>
        <div class="row">
            <div class="col-md-12">
                <ul class="bxslider_story">

                    @foreach (var item in Model)
                    {
                        <li class="s1">
                            <img src="~/Content/View_components/images/students/1.jpg" alt="img" data-toggle="tooltip" title="@item.Name">
                            @*<img src="@item.Logo" alt="img">*@
                            @*<p class="animate_bottom">@item.Email</p>*@
                        </li>
                   
                    }

                </ul>
            </div>
        </div>
    </div>
</div>
